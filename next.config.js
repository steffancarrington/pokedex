module.exports = {
    /*
        Custom Image Component Config
        - domains = specify which images to include from external resources
        - deviceSizes = sepcify responsive breakpoints to display different images at
        - imageSizes = specify a list of image widths to be displayed at different breakpoints
         Add in config settings to enable Next Image Comoponent on External image sources
    */ 
    images: {
      domains: ['assets.pokemon.com'],
    },
}